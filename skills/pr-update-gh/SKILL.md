---
name: pr-update-gh
description: 現在の Git ブランチに紐づく既存の GitHub PR を gh CLI で特定し、差分とコミット履歴から初見の読者にも分かる日本語の説明本文を作成して更新する。PR 本文を実際に書き換えてほしい依頼で使い、文章案の生成だけには使わない。
---

# 現在のブランチの PR 本文を更新する

現在のブランチに紐づくオープンな PR の本文を、変更の対象と内容が前提知識なしでも分かる文章へ更新する。
ユーザーがタイトルの更新も明示しない限り、PR のタイトルは変更しない。

## PR を特定する

1. Git リポジトリ内であることと、現在のブランチ名を確認する。
2. `gh pr view --json number,url,title,body,baseRefName,headRefName,state` を使い、現在のブランチに紐づく PR を取得する。
3. 取得した PR が現在のブランチを head に持つオープンな PR であることを確認する。

ブランチが取得できない、PR が見つからない、または PR がオープンでない場合は更新しない。
別のブランチや PR を推測で選ばず、判明した状態をユーザーへ伝える。

## 変更内容を調べる

PR のタイトル、既存本文、base ブランチ、コミット履歴、変更ファイル一覧、差分を確認する。
PR 上の実際の差分は `gh pr diff` を優先し、差分が大きい場合は変更ファイル一覧と重要な差分を絞って読む。

調査結果から、次を分けて整理する。

- 対象：変更される機能、画面、処理、利用者、文書など
- 内容：対象が以前の状態からどのように変わるか
- 理由：issue、既存本文、コミット、差分から根拠を確認できる変更目的
- 確認事項：レビュアーが確認できる具体的な動作や成果物

ファイルパスや実装手段は、変更を理解するために必要な場合だけ使う。
「ファイルを修正した」ではなく、その修正で何が変わるのかを書く。

既存本文にチケットリンク、関連 PR、注意事項、手動作業、チェックリスト、スクリーンショットなどの有効な情報がある場合は、新しい本文にも残す。
古くなったと差分から断定できない情報を黙って削除しない。

## 本文を書く

本文には、少なくとも次の2セクションを含める。

```markdown
## このPRの対象

- 変更の対象を具体的に書く

## このPRで行うこと

- 対象がどのように変わるかを書く
```

理由や確認事項が読者の理解に役立ち、根拠もある場合だけ、`変更する理由` や `確認してほしいこと` などの短いセクションを追加する。
リポジトリに PR テンプレートや記述規則がある場合は、その構成を優先しつつ「対象」と「内容」が明確になるように書く。

文章は次の基準で整える。

- 初めて変更を見る人が「何が、どう変わるか」を理解できる言葉を使う。
- 専門用語や略語が必要なら、最初に短く意味を補う。
- 一文には一つの要点だけを書き、長い文を避ける。
- 箇条書きは、主語と文末の形をできるだけそろえる。
- 「改善」「対応」「最適化」などの曖昧な言葉だけで済ませず、変化を具体的に書く。
- 内部整理だけで利用者から見える動作が変わらない場合は、そのことを明記する。
- 目的、影響、互換性、テスト結果を推測で補わない。
- テストを実行していない場合は、実行済みであるかのように書かない。

## PR を更新して確認する

完成した本文を安全な一時 Markdown ファイルへ保存し、PR 番号を明示して次の形式で更新する。

```bash
gh pr edit <PR番号> --body-file <本文ファイル>
```

更新後は `gh pr view <PR番号> --json url,body` で本文を再取得し、意図した内容が反映されたことを確認する。
更新に失敗した場合は別の PR へ切り替えたり、同じ操作を無制限に繰り返したりせず、エラーを報告する。

最後に、更新した PR の番号と URL、および本文に書いた「対象」と「内容」を簡潔に報告する。
